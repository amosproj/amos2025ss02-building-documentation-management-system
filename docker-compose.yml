version: '3.8'

services:
  web:
    image: httpd:latest
    container_name: bitbeam-web
    ports:
      - "80:80"
    volumes:
      - ./BitAndBeam/web-content:/usr/local/apache2/htdocs/
    restart: always

  # Placeholder for future services
  # database:
  #   image: postgres:14
  #   container_name: bitbeam-db
  #   environment:
  #     POSTGRES_PASSWORD: ${DB_PASSWORD}
  #     POSTGRES_USER: ${DB_USER}
  #     POSTGRES_DB: ${DB_NAME}
  #   volumes:
  #     - postgres-data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
  #   restart: always

  # opensearch:
  #   image: opensearchproject/opensearch:latest
  #   container_name: bitbeam-search
  #   environment:
  #     - discovery.type=single-node
  #   ports:
  #     - "9200:9200"
  #     - "9600:9600"
  #   restart: always

# volumes:
#   postgres-data:
